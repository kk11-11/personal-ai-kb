# 面试讲述稿：个人 AI 知识库项目

> 用途：AI 应用开发实习面试时，讲清楚"我做过什么、怎么做的、做出什么结果"。
> 方法：STAR 法（情境 Situation / 任务 Task / 行动 Action / 结果 Result）。

---

## 一句话介绍（电梯陈述，30 秒版）

> "我独立搭建了一个**基于 RAG 的个人 AI 知识库 Web 应用**。用户上传文档后，系统会自动做向量化建库；提问时，AI 会**只基于用户自己的资料**给出回答，并标注每段回答的出处来源。技术上用 Flask 做后端、bge-small-zh 本地向量化、Chroma 存向量、智谱 GLM-4-flash 生成答案。整个检索链路都是我手写跑通的。"

---

## STAR 详细版

### S - 情境 (Situation)
我准备找 AI 应用开发方向的实习，但缺少一个能体现"真正动手做过 AI 应用"的项目。市面上的教程要么只调 API、要么堆框架，我想做一个**小而完整、能跑、能演示**的东西，证明我理解 AI 应用落地的完整链路，而不只是会调接口。

### T - 任务 (Task)
目标：用 Python 从零搭一个本地可用的个人知识库，要求：
1. 支持上传文档并自动建库；
2. 提问时**基于资料回答**，不瞎编；
3. 回答要能**溯源**（标注来自哪份资料的哪一段）。

### A - 行动 (Action)
- **技术选型**：后端用 Flask（轻量、易讲清请求链路）；向量化用本地 `bge-small-zh-v1.5`（中文效果好、资料不出本机）；向量库用 Chroma；生成用智谱 GLM-4-flash（有免费额度）。
- **RAG 链路**：手写递归切分（带重叠窗口）→ sentence-transformers 生成句向量 → 存入 Chroma → 提问时检索 Top-K → 拼成带【资料】标记的 Prompt → 调大模型生成。
- **踩过的坑 & 解决**：
  - 国内网络拉不到 HuggingFace 模型 → 改用 ModelScope 下载到本地，设置离线环境变量，彻底不依赖外网；
  - Chroma 在"删除重建集合"后旧句柄失效导致查询报错 → 在重建后把全局 collection 句柄重新指向新集合，修复了检索崩溃。
- **工程化**：拆成命令行版（ingest/ask）和 Web 版（app.py + 前端页面），写了 README，可一键复现。

### R - 结果 (Result)
- 项目**完整跑通**：上传文档后能基于资料正确回答问题，并标注来源；
- 产出可写在简历上的**端到端 AI 应用** + 配套文档；
- 过程中**真正理解了 RAG 的每个环节**（切分、向量化、检索、生成），而不只是调包。

---

## 高频追问 & 应对

**Q1：为什么用 RAG 而不是直接问大模型？**
A：因为大模型不知道我的私有资料。RAG 让模型"先检索再回答"，既用上了私有知识，又能溯源、降低幻觉。

**Q2：chunk 切多大合适？为什么带重叠？**
A：太大检索不精准，太小丢上下文。我默认 500 字、重叠 80 字——重叠是为了避免一句话被切断在边界，保证语义完整。

**Q3：为什么 embedding 用本地模型，不用 API？**
A：一是隐私（资料不出本机），二是成本（本地免费、可离线），三是更可控。线上大批量时也可以换 API 版。

**Q4：Chroma 那个 bug 你怎么定位的？**
A：现象是上传后一提问就报 "Collection does not exist"。排查发现 `reingest()` 删了旧集合又建了新的，但全局句柄还指着被删的旧对象。修复方式是重建后把全局 `collection` 重新赋值。说明我读得懂报错、能定位到状态管理问题，而不只是 copy 代码。

**Q5：这个项目和实习岗位有什么关联？**
A：AI 应用开发的核心就是把大模型"接进真实业务 + 管好私有数据 + 控住成本与准确性"，RAG 正好是最典型的落地形态，我这个项目把这条链路完整走了一遍。

---

## 演示准备（3 分钟现场版）

1. 提前启动 `python app.py`，浏览器停在 http://127.0.0.1:5000；
2. 演示：上传 `docs/` 里一篇笔记 → 重建 → 问"RAG 是什么" → 展示**带出处的回答**；
3. 顺势讲一句："回答里 `02_rag_basics.md#0` 就是来源引用，这是 RAG 相比纯聊天最大的价值——可溯源、不瞎编。"
4. 收尾："整套链路我都是手写跑通的，技术细节和踩坑都能展开讲。"

---

_提示：面试前用 `python verify_embedding.py` 和 `python test_llm.py` 各跑一遍，确认环境没问题，避免现场翻车。_
