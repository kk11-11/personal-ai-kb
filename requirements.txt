# 直接依赖(已冻结版本,与本地 venv 一致,避免云端装到不兼容新版)
flask==3.1.3
chromadb==1.5.9
sentence-transformers==6.0.0
pdfplumber==0.11.10
python-docx==1.2.0
openai==3.3.1
modelscope==1.39.1
gunicorn==26.2.0
# Agent 编排框架: 第1周用 LangGraph 重写手写 ReAct 循环(见 langgraph_agent.py / LANGGRAPH_NOTES.md)
# 说明: 本机 venv 因环境限制将框架装在 venv/langgraph_extra 侧目录(由 .pth 自动挂载),
#       在干净机器上直接 `pip install -r requirements.txt` 即可正常装入 site-packages。
langgraph==1.2.11
langchain-core==1.6.1
# 关键间接依赖锁定(云端若拉到不兼容新版会崩,本地已验证以下组合可用)
numpy==2.5.2
# 注意:torch 刻意不在此安装 —— 由 Dockerfile 先从 PyTorch 官方 CPU 源安装
# torch==2.13.0+cpu。PyPI 上默认的 torch 是 CUDA 版(约 2~3GB),本项目为纯 CPU
# 推理、完全用不上 CUDA 运行时;CPU 版仅约 200MB,镜像体积可降一个数量级。

