# 直接依赖(已冻结版本,与本地 venv 一致,避免云端装到不兼容新版)
flask==3.1.3
chromadb==1.5.9
sentence-transformers==6.0.0
pdfplumber==0.11.10
python-docx==1.2.0
openai==3.3.1
modelscope==1.39.1
gunicorn==26.2.0
# 关键间接依赖锁定(云端若拉到不兼容新版会崩,本地已验证以下组合可用)
numpy==2.5.2
torch==2.13.0

