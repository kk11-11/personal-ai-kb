# 虚拟环境 / Python 缓存
venv/
.venv/
__pycache__/
*.pyc

# 向量库:运行时从 docs/ 重建(秒级),不打进镜像
chroma_db/

# 模型:git 仓库里不含(在 .gitignore 中),HF/PaaS 启动时由 ensure_model/ensure_reranker
# 从 ModelScope 下载 —— bge-small-zh-v1.5 约 92MB 同步下载(几十秒,保证首次查询可用),
# bge-reranker-base 约 1GB 改后台线程下载(不阻塞启动,期间用纯向量检索 94.4%,
# 下载完自动升级到二阶段重排 100%)。详见 app.py:ensure_reranker(async_download=True)。
models/

# 密钥 / 运行产物
.env
*.env
feedback.json
*.log
run.log

# 隐私文件(本地简历/面试稿,绝不进镜像)
面试讲述.md
简历项目经历.md
项目简介.md

# git / 测试脚本(非运行必需)
.git/
test_*.py
debug_*.py
*_result*.txt
